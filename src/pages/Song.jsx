import { useParams, Link, Outlet } from "react-router-dom";


function Song() {
    const {songId} = useParams();

    return (
        <div>
            <nav>
                
            </nav>
        </div>
    )
}

export default Song;